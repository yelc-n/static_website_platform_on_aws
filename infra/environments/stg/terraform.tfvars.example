# Environment variable values for `stg`.
# Purpose: Provide concrete values for variables declared in `variables.tf` for the dev environment.
# NOTE: Do not store secrets in a tfvars file checked into source control.

env               = "stg"
project_name      = "name-of-project"
region            = "ap-northeast-1"
root_domain_name  = "stg.example.com"
sub_domain_names  = ["app"]
primary_zone_id   = "Z123456ABCDEFG" // Replace with your actual Route 53 Hosted Zone ID